# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment files — contain keys/endpoints, never commit
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Personal study / interview-prep notes — private, never committed
*.private.md

# Personal documents (CV, cover letters) — never committed to a public repo
*.docx

# AI assistant project instructions — private, not for the public repo
CLAUDE.md
